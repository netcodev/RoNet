{
  "name": "ronet",
  "version": "2.4.11",
  "description": "A fork of RoValra.",
  "private": true,
  "scripts": {
    "build": "npm run clean && node build.js",
    "clean": "rimraf dist",
    "lint": "eslint .",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "eslint": "10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.4.0",
    "patch-package": "^8.0.1",
    "prettier": "^3.8.1",
    "prettier-eslint": "^16.4.2",
    "rimraf": "^6.1.3",
    "sass": "^1.97.3"
  },
  "dependencies": {
    "dompurify": "^3.3.2",
    "marked": "^17.0.4",
    "roavatar-renderer": "1.2.0"
  },
  "lint-staged": {
    "*.js": "eslint --fix",
    "*.{js,json,css,md}": "prettier --write"
  }
}
